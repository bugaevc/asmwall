Addition and Subtraction
========================

## `ADD a, b`
Equivalent to `a += b` in C.
`a` and `b` can't both be memory.

## `SUB a, b`
Equivalent to `a -= b` in C.
`a` and `b` can't both be memory.
